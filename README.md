# TUI-Writer

> TUI for writing out to markdown files. Clean, minimalist, display with a
simple interface.

## TODOs

- ~~Allow opening arbitrary files through the CLI~~
- File history (individual lines changed per save). Kind of like git for your file.
- Autosaving after a period of time
- VIM Mode (wip)
- Configurable options
  - Autosave intervals
  - Editor Width
  - Font-Size
  - Opening separate files